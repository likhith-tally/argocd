# Example values.yaml : https://github.com/external-secrets/external-secrets/blob/main/deploy/charts/external-secrets/values.yaml

replicaCount: 1

# image:
#   repository: "ocir.ap-mumbai-1.oci.oraclecloud.com/bmwva1xqkb3s/oss-images"
#   pullPolicy: IfNotPresent
#   tag: "external-secrets-v0.17.0"

# [TODO] Uncomment and set proper values
# resources: {}

podLabels:
  component-name: external-secrets-operator
nodeSelector:
  oke.oraclecloud.com/pool.name: grafana
topologySpreadConstraints:
- maxSkew: 1
  topologyKey: kubernetes.io/hostname
  whenUnsatisfiable: DoNotSchedule
  labelSelector:
    matchLabels:
      component-name: external-secrets-operator

certController:
  replicaCount: 1

  # image:
  #   repository: "ocir.ap-mumbai-1.oci.oraclecloud.com/bmwva1xqkb3s/oss-images"
  #   pullPolicy: IfNotPresent
  #   tag: "external-secrets-v0.17.0"

  # [TODO] Uncomment and set proper values
  # resources: {}

  podLabels:
    component-name: external-secrets-cert-controller
  nodeSelector:
    oke.oraclecloud.com/pool.name: grafana
  topologySpreadConstraints:
  - maxSkew: 1
    topologyKey: kubernetes.io/hostname
    whenUnsatisfiable: DoNotSchedule
    labelSelector:
      matchLabels:
        component-name: external-secrets-cert-controller

webhook:
  replicaCount: 1

  # image:
  #   repository: "ocir.ap-mumbai-1.oci.oraclecloud.com/bmwva1xqkb3s/oss-images"
  #   pullPolicy: IfNotPresent
  #   tag: "external-secrets-v0.17.0"

  # [TODO] Uncomment and set proper values
  # resources: {}

  podLabels:
    component-name: external-secrets-webhook
  nodeSelector:
    oke.oraclecloud.com/pool.name: grafana
  topologySpreadConstraints:
  - maxSkew: 1
    topologyKey: kubernetes.io/hostname
    whenUnsatisfiable: DoNotSchedule
    labelSelector:
      matchLabels:
        component-name: external-secrets-webhook
